<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="MyTheme" parent="Base.ThemeOverlay.AppCompat.Light">
        <item name="android:actionBarWidgetTheme">@style/MyTheme</item>
        <item name="actionBarWidgetTheme">@style/MyTheme</item>
    </style>
</resources>
